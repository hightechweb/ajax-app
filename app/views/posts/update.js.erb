// Close the modal for update.js.erb
$('#dialog').modal('toggle');

// Add fade out animation to post and prepend updated post from DOM after animation
$('#post_<%= @post.id %>').fadeOut(500, function(){
	$(this).remove();
	$('#container_posts').prepend('<%= j render @post %>');
});